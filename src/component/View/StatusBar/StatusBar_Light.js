
import { StatusBar, } from 'react-native';
const StatusBar_Light = () => {
    return (
        <StatusBar barStyle={"dark-content"} backgroundColor={'white'} />
    );
};

export default StatusBar_Light;
